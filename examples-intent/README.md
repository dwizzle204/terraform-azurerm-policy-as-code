# Intent Example (data-driven)

Demonstrates YAML-driven policy assignment intent (#13). See
`../modules/intent/README.md` for the full contract.

`assignments.yaml` is optional — when absent the module receives an empty map,
so `terraform validate` works offline.
